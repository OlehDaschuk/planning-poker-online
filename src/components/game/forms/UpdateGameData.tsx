import React from 'react';
import { TextField, Button } from '@mui/material';

export const UpdateGameData: React.FC = () => {
  return (
    <form>
      <Button>Udate game settings</Button>
    </form>
  );
};
